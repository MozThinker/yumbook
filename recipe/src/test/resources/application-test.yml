server:
  port: 8080
spring:
  application:
    name: recipe
  datasource:
    username: 'yumbook'
    url: jdbc:postgresql://localhost:5432/recipe_test
    password: 'password'
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    hibernate:
      ddl-auto: create-drop
    show-sql: true

cors:
  allowedOrigins:
    http://localhost/